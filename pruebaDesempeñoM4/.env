# Configuración del Servidor
PORT=3000

# Conexión a PostgreSQL (Asegúrate de que el puerto 5433 sea el de tu Docker)
POSTGRES_URI="postgresql://LI:12345@localhost:5433/pruebaDesempeñoP"

# Conexión a MongoDB (Local o Docker)
MONGO_URI="mongodb://localhost:27017/pruebaDesempeñoM"

# Ruta del archivo de datos (Verifica que la carpeta 'data' exista con el archivo)
FILE_DATA_CSV="./data/AM-prueba-desempeno-data_m4.csv"